{
  "name": "heroku-ts-cli-server",
  "version": "1.0.0",
  "description": "Minnimal deployment of daf-cli server to Heroku",
  "repository": "https://github.com/uport-project/daf-deployments",
  "author": "jason.healy@mesh.xyz",
  "license": "Apache-2.0",
  "scripts": {
    "start": "NODE_ENV=production yarn daf server -p $PORT --config=./config/agent-heroku.yml",
    "dev": "yarn daf server --config=./config/agent-local.yml"
  },
  "dependencies": {
    "daf-cli": "^7.0.0-beta.45"
  }
}
